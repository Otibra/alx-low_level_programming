#include "main.h"

#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result
 *
 * Return: result
 */

int add(int a, int b)
{
	int result;

	result = (a + b);
	return (result);
}
