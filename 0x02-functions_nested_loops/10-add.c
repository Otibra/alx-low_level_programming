#include "main.h"

#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result
 *
 * @a: added integer
 * @b: added integer
 * Return: result
 */

int add(int a, int b)
{
	int result;

	result = (a + b);
	return (result);
}
